# PR レビュー: PRJ-test 喫茶まほろば LP

日時: 2026-04-18  
レビュアー: Claude  
対象ファイル: `src/index.html`, `src/style.css`, `src/script.js`

---

## 5観点レビュー

### セキュリティ

問題なし。

- シークレットのハードコードなし
- Instagram リンクに `rel="noopener noreferrer"` を付与済み（`index.html` L148）
- `href="#"` のダミーリンクには `target="_blank"` がなく `rel` 属性は不要
- 実 URL 設定時の `rel` 追加リマインダーが TODO コメントとして記載済み（`index.html` L27, L42, L124）
- Google Maps の `referrerpolicy="no-referrer-when-downgrade"` は標準的な記述

### 堅牢性

防御的実装を 3 点確認。

- `IntersectionObserver` 非対応ブラウザへのフォールバック: `script.js` L20–22
- JS 無効時のコンテンツ強制表示: `style.css` L464–468 `@media (scripting: none)`
- スクロールイベントへの `{ passive: true }` 付与: `script.js` L11

### 要件適合

F-01〜F-06 すべて実装済み。

| 機能ID | 内容 | 判定 |
|--------|------|------|
| F-01 | ヒーローセクション + CTA | ✓ |
| F-02 | コンセプト紹介（画像・本文） | ✓ |
| F-03 | メニューハイライト 3 品（税込価格） | ✓ |
| F-04 | アクセス・Google マップ埋め込み | ✓ |
| F-05 | CTA ボタン（ダミーリンク） | ✓ |
| F-06 | スムーススクロール（CSS） | ✓ |

### テスト

`tests/` ディレクトリが空。要件定義 §11 Phase 1 に記載の Playwright テストが未作成。Phase 2 繰越として記録する。

### 可読性

良好。

- CSS カスタムプロパティによるデザイントークンの集約管理（`style.css` L6–34）
- セマンティック HTML: `<nav>` `<main>` `<section>` `<article>` `<dl>` `<footer>`
- `heroZoom` アニメーションの初期値を `scale(1.05)` から開始し、ロード時のガクつきを防止（`style.css` L219–224）

---

## 未解決の指摘事項（TODO）

- [ ] `tests/` に Playwright テストを追加する（Phase 2 で対応）
- [ ] 本番リリース前に CTA リンク・Google Maps Place ID を実 URL へ差し替える

---

## 生成した PR 文案

### PR タイトル

```
feat(cafe-lp): 喫茶まほろば LP 初期実装（静的1ページ）
```

### PR 本文

```md
## 概要

架空のカフェ「喫茶まほろば」のシングルページ LP を HTML / CSS / Vanilla JS で実装し、Netlify デプロイ可能な静的ファイル構成として完成させた。

## 背景

PRJ-test の要件定義書（requirements.md §1〜§6）に基づく Phase 1 実装。ターゲットユーザーが 30 秒以内に店の世界観を把握し、CTA から予約に進める LP を目標とする（§2）。

## 変更点

- `src/index.html`: Hero / Concept / Menu / Access / Footer の 5 セクション構成、セマンティック HTML
- `src/style.css`: デザイントークン管理（CSS カスタムプロパティ）、375px〜1440px レスポンシブ対応、ガラスモーフィズムナビ、フェードインアニメーション
- `src/script.js`: Intersection Observer によるスクロールフェードイン、IntersectionObserver 非対応ブラウザへのフォールバック

## 動作確認

- [ ] ブラウザ幅 375px でヒーロー CTA ボタンがファーストビューに収まることを確認する
- [ ] Chrome / Safari / Firefox の最新バージョンで全セクションの表示崩れがないことを確認する
- [ ] JS を無効化した状態でコンテンツが全て表示されることを確認する（scripting: none フォールバック）
- [ ] キーボード（Tab キー）のみで CTA ボタンまで到達できることを確認する

## レビュー依頼点

- モバイル幅（768px 以下）でナビリンクを非表示にし CTA ボタンのみ残す実装を採用している（`style.css` L497–504）。ハンバーガーメニュー未実装の MVP 対応としているが、デスクトップとの体験差を許容するかの判断を確認したい
- Google Maps 埋め込みは仮の Place ID を使用している（`index.html` L128）。本番リリース前に差し替えが必要

## 関連

- Closes #（Issue なし・テスト案件）
- requirements: `projects/PRJ-test_カフェLP/requirements.md`
```

### コミットメッセージ

```
feat(cafe-lp): 喫茶まほろばLPの初期実装

HTML/CSS/Vanilla JS による静的1ページLPを実装する。
Hero・Concept・Menu・Access・Footerの5セクション構成。
CSS カスタムプロパティでデザイントークンを管理し、
Intersection Observer でスクロールフェードインを実現する。

- レスポンシブ対応: 375px〜1440px
- IntersectionObserver 非対応ブラウザへのフォールバックを追加
- JS 無効時の表示保証 (@media scripting: none)
- ハンバーガーメニューは MVP スコープ外として未実装

Refs: projects/PRJ-test_カフェLP/requirements.md
```
